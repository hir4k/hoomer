package Factorial

fn factorial(number)
    if number <= 1
        return 1
    end

    return number * factorial(number - 1)
end

fn main
    for number in 0..10
        print "{number}! = {factorial(number)}"
    end
end
