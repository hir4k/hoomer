fn print_fibonacci(remaining, current: 0, next: 1)
    if remaining <= 0
        return
    end

    print(current)
    print_fibonacci(
        remaining - 1,
        current: next,
        next: current + next,
    )
end

fn main
    print_fibonacci(12)
end
